# brain — project brain (L2)

@~/.claude/brain/thinking.md

> このファイルは PJ 固有の判断軸（L2: project brain）。
> 上記 `@import` で L1 (personal brain) を取り込み、L1 が transitively L0 (base brain) を取り込む。
> 詳細は ADR-0013 参照。
>
> ホーム L1 が存在しない場合、上記 @import 行を `@brain/thinking.md` に置き換えると L0 を直接取り込める。
> PJ 固有の判断軸はこのファイル下部に追記する。`/setup` Step 4.5 の選択肢 (a)(b)(c) に応じて、このファイルの構成を変更してよい。
