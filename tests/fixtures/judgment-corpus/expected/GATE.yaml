# 凍結判定: 2026-07-04 / claude-fable-5（Mythos-class）
# 変更禁止（append-only）。被測定モデルには絶対に見せない。

- id: GATE-01
  decision: "NO-GO"
  failing_items: [2, 3, 4, 5]
  note: "6 を『影響範囲未定のため機械判定不能 → 安全側 NO』として含める回答も正答扱い（判定不能は NO に倒す規約のため）。2/3/4/5 を全て挙げていることが必須条件"
  rationale: |
    R1-2: 「A案/B案どちらでも」は未決定マーカー。R1-3: 完了条件が観測不能
    （「精度を改善」は実行可能なコマンド・テスト・画面のいずれにも言及しない）。
    R1-4: 「いい感じ」は曖昧語。R1-5: 影響範囲が「未定」。
    設計判断が残った Issue の無人実行は R2 難所（設計判断）の無人化に等しい。

- id: GATE-02
  decision: "GO"
  failing_items: []
  rationale: |
    7 項目すべて YES: Issue 本文あり / 未決定マーカーなし / 完了条件が
    実行可能コマンド + 期待出力 + exit code で観測可能 / 曖昧語なし /
    影響範囲 1 ファイル 1 行を列挙 / DB 変更なし明記 / Active Work 重複なし明記。
