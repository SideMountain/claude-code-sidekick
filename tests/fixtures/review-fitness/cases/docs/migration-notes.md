Negative control: prose about migrations must stay out of the scan (pathspec scope).

We will DROP TABLE legacy_users and RENAME the amount column.

<img src="doc.png">
<input name="q">

```ts
try { boot() } catch (e) {}
```
