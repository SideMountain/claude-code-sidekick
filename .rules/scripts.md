---
globs: ["scripts/**"]
---

# データパッチ / バッチスクリプト ルール

scripts/ 配下のファイルはDBデータを操作する可能性がある。
CLAUDE.md §2 HARDルール（H1: 接続先確認、H2: dry-run、H6: 手順提示+承認）が全て適用される。

追加ルール:
1. **dry-run**: `--dry-run` オプションがあれば先に実行結果を確認
2. **影響行数**: 事前に SELECT で対象件数を確認・報告
3. **実行結果**: 重要な判断があれば ADR に記録する
